{
  "//": "Compiles the ECS core plus the few PURE server rules that need no world or socket (loot eligibility), for the self-test. Kept separate from the server/client configs so the ECS can be typechecked and exercised in about a second without building the whole game.",
  "//codegen": "target ES2020 + useDefineForClassFields:false keep the emitted JS fast. ES5 downlevelling would turn async into generator state machines and for..of into iterator-protocol calls; [[Define]] class-field semantics (the default from ES2022) add a shape transition per field on per-frame objects. Pinned so a target bump cannot silently change the emit.",
  "compilerOptions": {
    "target": "ES2020",
    "useDefineForClassFields": false,
    "module": "CommonJS",
    "lib": ["ES2020", "DOM"],
    "outDir": "./dist_ecs_test",
    "rootDir": "./src",
    "strict": true,
    "noUnusedLocals": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true,
    "resolveJsonModule": true,
    "moduleResolution": "node",
    "types": ["node"]
  },
  "include": ["src/ecs/**/*", "src/server/shared/botId.ts", "src/server/shared/lootEligibility.ts", "src/server/shared/lootEligibility_self_test.ts"]
}
