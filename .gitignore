node_modules
# dist/ is committed on purpose: the in-game `update` command installs from the
# GitHub branch zipball, and a fresh clone should run without a build step.
# The live database must never be committed, though:
dist/inventory.json
public_build_florrclone.zip
db_backups/
src/inventory.json
server_*.log
server_*.pid
.DS_Store
**/.DS_Store
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
dist_ecs_test/
dist_bench/
